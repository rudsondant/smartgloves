#pragma strict

//Script Não Utilizado - Função Desenhar circuito do jogo
var qtdNewMapa : int = 5;
var mapa : GameObject;
var newMapa : GameObject[];
var i: int=0;
function Update () {
	var transformMapa = gameObject.transform;
	for(i = 0; i < qtdNewMapa;i++){
		//newMapa = new mapa();
		//transformMapa.position.x = -59.463*(i+2);
		//GameObject.Instantiate(gameObject,transformMapa.position,transformMapa.rotation);
	}
}